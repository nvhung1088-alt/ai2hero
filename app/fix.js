require('dotenv').config({path: '.env'}); const postgres = require('postgres'); const sql = postgres(process.env.POSTGRES_URL); sql.unsafe(\SELECT id, status FROM dub_tasks WHERE id=28\).then(res => { console.log(res); process.exit(); });
