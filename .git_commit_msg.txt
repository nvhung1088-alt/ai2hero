feat: launch AI2Hero Platform MVP & integrated services

- Core Architecture & Infrastructure:
  - Migrate and configure Next.js to stable 15.5.19, setup Sentry SDK monitoring.
  - Setup code standards with ESLint and Prettier configurations.
  - Upgrade Connect Hub API Gateway: integrate Anthropic, Gemini, OpenAI, DeepSeek, ChiaSeGPU, Luma, Runway connectors, and support custom model discovery.

- HeroDub MVP (Video Translation & AI Voice-Over):
  - Database schema for `dubWorkers` and `dubTasks` tables.
  - API routes for workers, tasks, and presigned URLs.
  - Independent Python local worker implementation with downloader, translator, vocal isolation (Demucs), timing sync (atempo), silence trimming, and background mix.
  - Advanced Obsidian Glassmorphism Dashboard UI and setup guide.

- HeroFilm MVP (Vertical Drama & Watch Player):
  - Database schema for `film_series`, `film_episodes`, `film_watch_history`, `film_bookmarks`, `film_ratings`, `film_transactions`, and `film_reports`.
  - Creator CMS for series administration, episode management, and watch reports.
  - Watch Player with Custom Control Bar, Aspect Detection, Smart Fullscreen, and Soft-DRM protection.
  - Token-based monetization integration & YouTube Sync with AI description generation.

- HeroVideoMaker MVP (AI Video Generation):
  - Timeline & track database schema (`videoMakerAssets`, `videoTracks`, `videoStoryboards`).
  - Orchestrator and supervisor agent system with multi-agent pipeline (Script, Asset, Storyboard agents).
  - Integration with local Electron renderer via WebSocket bridge & SSE agent chat streaming.
  - UI client pairing widget for local-device authorization.

- HeroCare MVP (Omnichannel CSKH Agent):
  - Database schema for inboxes, scripts, snapshots, customers, and daily resets.
  - Server actions CRUD, AI Reply Engine with FAQ matching, and webhook forwarding.
  - Snapshot sync cron and daily quota resets.

- HeroReport MVP (Automated Business Reporting):
  - POS data metric aggregator and scheduler.
  - AI analysis and comments dispatching to Telegram/Zalo.

- HeroMarketplace MVP (Shop CMS & Fulfillment):
  - Integrated seller dashboard with inventory alerts, product/orders administration, and AI suggestions.
  - WebRTC-based camera recorder for packing verification & barcode scanner simulator.

- HeroSocial & HeroAgent MVP:
  - iSocial feeds, stories, messages, and friend suggestions.
  - Chrome Extension "Hero Agent" integration with database task routing.
  - Batch social scheduler for Facebook Pages and TikTok.
